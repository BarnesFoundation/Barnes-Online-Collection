# Shell scripts MUST stay LF. A CRLF run.sh fails in Lambda with "bad interpreter: /bin/bash^M"
# (same class of bug that silently broke the nightly refresh). Guard it regardless of core.autocrlf.
*.sh text eol=lf
