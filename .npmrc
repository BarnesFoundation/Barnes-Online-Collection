# React-16 / CRA-5-era dependencies carry malformed peer-dependency specs that modern npm
# rejects (EINVALIDTAGNAME) — skip strict peer resolution so install/ci succeed on Node 24.
legacy-peer-deps=true
